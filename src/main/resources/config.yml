spawnServer: "spawn"
languageFile: "lang.yml"
useLastServer: true
saveOnPostConnect: true
saveOnDisconnect: true

storage:
  type: "yaml" # yaml or mariadb
  yaml:
    file: "players.yml"
  mariadb:
    host: "localhost"
    port: 3306
    database: "velocity"
    username: "velocity"
    password: "password"
    table: "player_last_server"
    useSsl: false
